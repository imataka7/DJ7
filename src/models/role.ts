
interface Role {
  playerPause: boolean;
  playerSkip: boolean;
  playerSeek: boolean;
  addViaSearch: boolean;
  queueShift: boolean;
  queueSort: boolean;
  queueDelete: boolean;
  queueInterrupt: boolean;
  queueMoveToTop: boolean;
  addFromHistory: boolean;
  manageUser: boolean;
}

export default Role;

